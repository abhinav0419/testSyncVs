<aura:component controller="TestingController" implements="force:appHostable,flexipage:availableForAllPageTypes,flexipage:availableForRecordHome,force:hasRecordId">
        <aura:attribute name="testData" type="List" />
    <aura:attribute name="url" type="string" />
        <aura:handler name="init" value="{!this}" action="{!c.doInit}" />
        <table class="slds-table slds-table_bordered slds-table_striped slds-table_cell-buffer slds-table_fixed-layout">
          <thead>
            <tr class="slds-text-heading_label">
              <th scope="col"><div class="slds-truncate" title="ID">ID</div></th>&nbsp;&nbsp;
              <th scope="col"><div class="slds-truncate" title="Name">Name</div></th>
              <th scope="col"><div class="slds-truncate" title="Name">Frame</div></th> 
            </tr>
          </thead>
          <tbody>
            <!-- Use the Apex model and controller to fetch server side data -->
            <aura:iteration items="{!v.testData}" var="tt">
                <tr>
                    <td scope="row"><div class="slds-truncate" title="{!tt.NNN__c}">{!tt.NNN__c}</div></td> &nbsp;&nbsp;
                    <td><div class="slds-truncate" title="{!tt.Name}">{!tt.Name}</div></td>
                    <td><iframe src="{!tt.Name}" /></td>
                </tr>
            </aura:iteration>
          </tbody>
        </table>
      </aura:component>